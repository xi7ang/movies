name: 'Movies Notify'
description: 'Notification action for movies repository'
author: 'mswnlz'
inputs:
  message:
    description: 'The message to print'
    required: false
    default: 'Hello from Movies Action!'
runs:
  using: 'node20'
  main: 'index.js'
